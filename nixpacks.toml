[phases.setup]
nixPkgs = ['nodejs_22', 'pnpm']

[phases.install]
cmds = ['pnpm install --frozen-lockfile']

[phases.build]
cmds = ['pnpm --filter @myfans/api build']

[start]
cmd = 'cd apps/api && node dist/index.js'

[variables]
NODE_ENV = 'production'
